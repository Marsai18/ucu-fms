# Copy to fleet_backend/.env OR add these to the repo root .env (both are loaded).

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=ucu_fleet_management

# If 5000 is busy, set PORT=5001 (or any free port) and set the repo root .env
# VITE_API_URL=http://localhost:5001/api to match.

# Optional: frontend origin for CORS when deployed
# FRONTEND_URL=https://your-app.vercel.app
